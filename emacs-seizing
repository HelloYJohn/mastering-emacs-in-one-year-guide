当我用emacs打开一个c++项目的文件，光标放在函数上面，按下Ctrl + ], emacs 会自动帮我grep 使用该函数的所有索引，但是我去C-x C-c 去关闭emacs时,
emacs 卡住了，显示的信息 Wrote /User/john-y/.emace.d/.session，而且各种关闭都不好使，只能kill掉，虽然可以C-g 回到正常，但是emacs 无法关闭，请问有什么
方式可以解决这个问题，或者不去保存.session
